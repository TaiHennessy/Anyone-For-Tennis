@model AnyoneForTennis.Models.ScheduleViewModel

@{
    ViewData["Title"] = "Control Panel";
}

<h1>Control Panel</h1>

<h2>Local Schedule</h2>
<p>
    Create for Local: <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocalSchedule.First().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocalSchedule.First().Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocalSchedule.First().Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.LocalSchedule)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ScheduleId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ScheduleId" asp-route-isLocal="true">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ScheduleId" asp-route-isLocal="true">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<h2>Main Schedule</h2>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MainSchedule.First().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MainSchedule.First().Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MainSchedule.First().Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.MainSchedule)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <!--Edit Removed for Now-->
                    <a asp-action="Details" asp-route-id="@item.ScheduleId" asp-route-isLocal="false">Details</a>
                    <!-- No delete link for main schedules -->
                </td>
            </tr>
        }
    </tbody>
</table>


<h2>Memberships</h2>
<h3>Members</h3>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Member.First().FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Member.First().LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Member.First().Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Member)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
            </tr>
        }
    </tbody>
</table>

<h3>Coaches</h3>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Coach.First().FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coach.First().LastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Coach)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
            </tr>
        }
    </tbody>
</table>

